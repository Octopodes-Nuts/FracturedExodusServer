# Testing

This directory is reserved for integration/load tests and fixtures.

- Unit tests live alongside Go source files in `src/`.
- Add HTTP collections, sample payloads, and docker-compose setups here.
